/** 
 1. optimizeDeps配置选项用来控制依赖优化行为：
    -->主要配置：
               entries：用于明确指定需要优化的入口文件
               include：指定需要被预构建的依赖
               exclude：是定不应该被预构建的依赖
               force：强制重新优化依赖，是个布尔值(即使Vite认为它们没有发生变化，这个选项可以强制在更新依赖或在node_modules中有文件变动后使用)
               
               










 **/ 